Expected font files (not yet added — placeholders only):

Poppins (English): Regular, Medium, SemiBold, Bold
Tajawal (Arabic): Regular, Medium, Bold

Once added, uncomment the `flutter.fonts` section in `pubspec.yaml`
(family names must match `lib/app/theme/nawa_typography.dart`).
