/// Placeholder for future Firebase configuration.
///
/// Firebase is planned for a later backend phase (auth, Firestore, etc.).
/// This file intentionally contains no Firebase SDK usage yet — it only
/// documents where project-specific config (from `flutterfire configure`,
/// e.g. `firebase_options.dart`) will be wired in once that phase starts.
class FirebaseConfig {
  const FirebaseConfig._();

  // TODO(nawa-backend-phase): initialize Firebase via generated
  // firebase_options.dart and call Firebase.initializeApp() during bootstrap.
}
